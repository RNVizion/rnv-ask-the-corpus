# Pinned 2026-08-06. Rationale, evidence, and the bump policy live in the corpus
# decision log (entry #3); this file stays short enough to read at a glance.
#
# Two tiers. The direct dependencies are ours to choose. The four transitive ones
# are pinned because they decide what a vector is: the embedding path runs
# sentence-transformers -> transformers -> tokenizers over torch and numpy, and
# a bump anywhere on it moves retrieval, which moves the gate.
#
# Bound, stated honestly: this is not a lock file. Transitive packages outside
# the list below still float, and three did on the first pinned run. None on the
# embedding path. The model weights are not pinned either; sentence-transformers
# fetches all-MiniLM-L6-v2 from the Hub at whatever revision it serves.

# --- direct ---
sentence-transformers==5.6.1
chromadb==1.5.9
beautifulsoup4==4.15.0
requests==2.34.2
anthropic==0.120.2
gradio==6.22.0
huggingface_hub==1.26.0

# --- transitive: these decide embedding values ---
transformers==5.14.1
tokenizers==0.22.2
torch==2.13.0
numpy==2.4.6
